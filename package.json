{
  "name": "resonance-terminal",
  "private": true,
  "version": "0.0.0",
  "description": "Resonance Terminal — crypto-only narrative intelligence terminal (private alpha).",
  "license": "Apache-2.0",
  "type": "module",
  "packageManager": "pnpm@10.14.0",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "test": "vitest run",
    "lint": "biome check .",
    "format": "biome check --write .",
    "verify": "pnpm lint && pnpm build && pnpm typecheck && pnpm test && node scripts/verify-packed-cli.mjs"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.10",
    "@types/node": "^26.2.0",
    "esbuild": "^0.25.0",
    "typescript": "^7.0.2",
    "vitest": "^4.1.11"
  }
}
