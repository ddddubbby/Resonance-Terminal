# Handoff

This file carries the integration agent's current milestone state so a fresh session in any client can continue without chat history. Feature agents report their own four fields in their PRs.

## Objective

Milestone `v0.1.0-alpha.1` — a usable, crypto-only private alpha built publicly on GitHub — is released: tagged `v0.1.0-alpha.1` on `main` after all fourteen branches of the [DESIGN.md](DESIGN.md) sequence merged. What the milestone still owes is recorded below.

## Current State

`main` carries fourteen merged branches. `bootstrap/repository-base`: the repository skeleton. `spike/ten-real-candidates` (PR #1): two live runs of the full loop on keyless sources, delivering `spike/candidates/2026-08-24-ten-candidates-v2.md`. `refactor/alpha-contracts` (PR #2): the stable contracts locked in `@resonance/lib` and DESIGN.md. `feat/canonical-snapshots` (PR #3): immutable git-trackable snapshot storage with schema-0.1 gating. `feat/market-connectors` (PR #4): Binance (via `data-api.binance.vision` because the trading host answers HTTP 451 here) and Coinbase market connectors with injectable fetch, plus raw-capture persistence at `<runDir>/raw/<connectorId>.json` (deterministic, immutable, failures recorded); live smoke returned 3,684 Binance tickers and 833 Coinbase products. `feat/research-connectors` (PR #5): DefiLlama, RSS/Atom, and GitHub release connectors, with the repo list re-curated for traction (dropped `polkadot/polkadot`, swapped the frozen `solana-labs/solana` for `anza-xyz/agave`, added `paradigmxyz/reth`); Robinhood Chain is covered via DefiLlama TVL since it has no machine-readable public channel; live smoke 19/19 ok. All five fixed connector families are now merged. `feat/corpus-clustering` (PR #6): the corrected clustering stage in `@resonance/lib` — exact cosine against term-sum centroids, news/release-only corpus, numeric bucketing, threshold recalibrated to 0.16 on the spike corpus; cross-source cluster share rose from 41.7%/11.9% to 75.7%/16.8%. Clusters are run-local derived views persisted as `<runDir>/clusters.json` with versioned configuration embedded (DESIGN.md records the stage-three decisions); the locked snapshot schema is untouched. `direction/narrative-granularity` (PR #7): a measured revision superseding the clustering-stage decisions — lexical clustering is demoted to deduplication and context reduction, event grouping moves agent-side in v1 (written rationale, model/rules stamps), an explicit event-to-theme layer owns cross-run narrative identity, and the corpus accumulates across runs. `feat/partial-scoring` (PR #8): the reworked 4A under that direction — `grouping.ts` (agent-side grouping record shapes, run-local `<runDir>/grouping.json`, `preGroupHints()` as the clustering module's demoted role), `narratives.ts` (the event-to-theme layer: `<storeDir>/narratives.json` with stable `n0001`… identities, library-side allocation, agent-side matching), and narrative-level partial scoring (`NarrativeObservation` per narrative per scan, per-narrative cold-start gate, honest availability, explicit coverage; 103 tests). `feat/agent-research-workflow` (PR #9): the research protocol as library support plus its normative document — `research.ts` (narrative evidence packs with bounded excerpts and an untrusted-data warning, market/TVL/alpha-signal reference sheets, `writeEvidencePacks` into `<runDir>/evidence/`), `withAllocatedNarrativeIds` (deterministic identity reattachment after ledger allocation), and `docs/PROTOCOL.md` recording the five-step scan (grouping as agent-side interpretation, identity/observation/evidence/scoring as deterministic; reproducibility split: bit-stable artifacts vs versioned interpretations; 115 tests). `feat/cli-scan-workflow` (PR #10): stage 4C — `normalize.ts` (the spike's proven rules rebuilt on the locked contracts: feed extraction, per-connector document builders, mover screening, dedup via the locked contentHash), `scan.ts` (one scan = fetch → normalize → snapshot → cluster; the run directory is the snapshot directory, so `raw/`, `clusters.json`, `grouping.json`, and `evidence/` sit beside `snapshot.json`; locked exit semantics), `promotions.ts` (append-only operator promotion ledger — a decision, never a threshold), and the packed CLI commands `scan`/`candidates`/`status`/`promote` with `--store`/`--json`; `candidates` rederives allocated narrative identities deterministically and reports honest availability; grouping stays agent-side per the protocol. Build change: `dist/index.js` is esbuild-bundled because the packed-tarball smoke check runs from an isolated temp directory (143 tests). `feat/agent-install-and-handoff` (PR #11): stage 5 — `scripts/install.sh` (idempotent one-command installer: Node 22/pnpm 10 checks, `--frozen-lockfile`, gated by `pnpm verify`; no API keys) and the `resonance handoff` command (deterministic agent-to-agent handoff document: canonical docs to read first, store summary, narrative table with honest scores, protocol reminders; writes nothing), closing milestone loop items 1 and 8; PROTOCOL.md gains the handoff protocol, README gains a truthful installation and command reference (146 tests). `test/private-alpha-e2e` (PR #12): stage 6 — `packages/cli/test/e2e.test.ts`, an offline integration suite wiring the complete protocol (scan, agent-side grouping, identity, observations, evidence packs, scoring, promotion, handoff) with two fake connectors and an injected clock; three scans span fifteen days so the per-narrative cold-start gate opens honestly inside the suite, and the CLI is exercised on the populated store (149 tests). A live smoke (not committed) ran the same chain twice on real endpoints: 24/24 connectors ok per scan, 242 textual documents, 20 agent-grouped narratives with written rationale (`n0001`…`n0020`), 20 observations, 20 evidence packs, and the full CLI chain including promotion and handoff; scoring honest throughout — attention components report cold-start, and marketConfirmation/investability score zero where the seed known-asset vocabulary has no overlap with screened movers, a known versioned limitation rather than a defect. `release/v0.1.0-alpha.1` (PR #13): stage 7 — version `0.1.0-alpha.1` across the root package, `@resonance/lib`, `@resonance/terminal`, and the CLI `VERSION` constant; `docs/RELEASE-NOTES.md` recording the milestone loop exactly as proven (items 1–4 and 7–8 done; item 5 — three scans across seven days — not done live, two same-session scans plus the integration suite's injected-clock three-scan run; item 6 proven offline and partial live); known limitations stated; the packed smoke check reads the tarball name from `packages/cli/package.json` instead of hardcoding it; annotated tag `v0.1.0-alpha.1` on the release merge. No snapshot data is committed yet. The GitHub milestone and branch-contract issues were never created; PRs #1–#13 proceeded directly and are all merged. `fix/alpha-security-blockers` (PR #14): four confirmed alpha security blockers closed without touching snapshot schema `0.1`, CLI commands, scoring, or source policy — snapshot write and read now recompute every document's SHA-256 identity and reject forged or stale `contentHash`/`docId` (`corrupted-snapshot`); `runDirOf` validates the runId against the locked `RUN_ID_PATTERN` before joining, so a traversal-shaped `--run` exits 1 without touching a path outside the store; the new `payloads.ts` validates successful captures per fixed connector family before persistence and normalization, turning a malformed HTTP-200 capture into a recorded failure that keeps id/url/status/timestamp and drops the payload while other connectors keep running (scan degrades, exit 2); and `HttpConnectorOptions` gains `maxResponseBytes` (`DEFAULT_MAX_RESPONSE_BYTES` = 16 MiB) enforced on the decompressed stream with `Content-Length` pre-checked when present, JSON parsed only after the bounded body is collected, and oversized or invalid responses recorded as failures preserving the HTTP status. 170 tests. `feat/asset-resolution` (PR #15): a live scan on `2026-08-28` surfaced the release's known limitation as a concrete failure — 28 grouped, evidence-backed narratives and not one usable score (0.000 or none, all 28). Root cause was mention resolution: a nine-string seed vocabulary against a 932-symbol tradeable universe, raw substring matching (`"Ethena"` matched `eth`), and a lowercase-name-vs-uppercase-ticker mismatch that made `marketConfirmation`/`investability` structurally incapable of scoring non-zero. `packages/lib/src/assets.ts` (rules version `2`) fixes all three: the vocabulary derives from the snapshot itself (market documents for the tradeable universe, TVL documents for protocol-name aliases), matching is word-boundary and longest-alias-wins, and the deny list was built by measuring real mis-resolutions rather than guessed. A second defect — mover documents deduplicated away by the market row for the same asset, both sharing a URL — is why the alpha-signals sheet was always empty; fixed by giving movers a distinct URL. Against the unchanged snapshot: 18 of 28 narratives now score; the ENA-buyback narrative goes `0.000` → `0.667`. The branch also adds the missing user-facing surface: `resonance brief` (off-radar movers, then confirmed narratives, then stated limits) as `docs/PROTOCOL.md` step 6, required by `AGENTS.md` after every scan; `candidates` now ranks and truncates to the top 10 by default instead of printing all 28 narratives with every component (`docs/DESIGN.md`, "Asset resolution and the run briefing"). 197 tests across 19 files.

## Next Action

Cut `release/v0.1.0-alpha.2` from `main`. Two branches have merged since the `v0.1.0-alpha.1` tag and neither is covered by its release notes: `fix/alpha-security-blockers` (PR #14, the four security fixes) and `feat/asset-resolution` (PR #15, mention rules `2` and the run briefing). The alpha.1 known limitation "seed asset vocabulary — marketConfirmation and investability therefore score zero on most real narratives today" is now false and must be rewritten, not carried forward.

The known-asset-vocabulary extension owed since the release is done (`feat/asset-resolution`). What remains from the milestone loop: the multi-day live soak (at least three scans across seven days against a committed store, grouping and observing each run until a narrative's cold-start gate opens) is still not proven live, only offline with an injected clock.

Two follow-ups were scoped but deliberately not started on this branch, since each changes locked or approved contracts and needs its own approved PR under `AGENTS.md`'s ground rules:

- **Price backfill and a momentum split.** `momentum` (30% weight) is entirely cold-start-gated today, so a first scan on a fresh store still cannot produce a full score. Splitting it into a backfillable price-momentum component and an attention-momentum component that stays gated would change `COMPONENT_WEIGHTS` — a score-rule change.
- **The event-to-theme layer.** Narrative granularity is deliberately event-level today (`docs/DESIGN.md`, "Narrative granularity"); a theme layer folding events into cross-run, cross-event narratives is the thing that would let momentum mean something beyond a single headline. Substantial: a new protocol step and ledger.

`investability` is also worth flagging: measured on the live run, every scored narrative gets exactly `1.0`, because `buildAssetIndex` only ever registers tickers with an exchange quote, so a resolved asset is tradeable by construction. It currently carries 10% weight for zero discriminating information; a liquidity-weighted redefinition (using the tape's `quoteVolume`) is the natural fix but is itself a score-rule change.

## Verification

`pnpm verify` is green on `main` at the release tag (Biome clean, both packages build, typecheck clean, 149 tests across 16 files pass, the esbuild-bundled packed CLI answers `--help` from an isolated temp directory); `node packages/cli/dist/index.js --version` prints `0.1.0-alpha.1`; the annotated tag `v0.1.0-alpha.1` points at the release merge. `pnpm install --frozen-lockfile` passes; the one-command installer (`scripts/install.sh`) was run live to completion (exit 0) with the full verify gate. Live smoke of the merged connectors: 19/19 research endpoints ok. Stage 6 live smoke on real endpoints: two scans with 24/24 connectors ok each, 242 textual documents, 20 agent-grouped narratives (`n0001`…`n0020`), 20 evidence packs, and the full CLI chain including promotion and handoff. End-to-end protocol smoke on the spike corpus (484 docs): 37 lexical hints, five groups with rationale, identities `n0001`…`n0005` allocated, observations appended, honest scores (cold-start and missing-input reported), five evidence packs plus three reference sheets rendered. Offline CLI smoke: `status` renders an empty store, `candidates` exits 1 with a clear next-action message, `handoff` renders an empty store honestly. On the spike branch the full pipeline ran live: 42/43 requests ok, 484 docs, 253 clusters, 215 bundles, ten v2 candidates.

`feat/asset-resolution`: `pnpm verify` is green (Biome clean, both packages build, typecheck clean, **197 tests across 19 files**, packed CLI smoke). Re-scored against the real, unchanged `2026-08-28T13-48-29` live snapshot and grouping (not a fixture): 18 of 28 narratives score where 0 of 28 did before; `resonance brief` names 8 off-radar movers (HEMI +36.67%, EDEN +20.87%, CHIP +16.00%, …) and 4 confirmed narratives (ENA, TRUMP ×2, ZRO) where the previous output was 196 unranked lines of zeros.
